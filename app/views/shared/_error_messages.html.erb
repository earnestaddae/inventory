<% if obj.errors.any? %>
<div style="color: red">
  <h4>Form has <%= pluralize(obj.errors.count, "error") %></h4>

  <ul>
    <% obj.errors.each do |error| %>
    <li><%= error.full_message %></li>
    <% end %>
  </ul>
</div>
<% end %>